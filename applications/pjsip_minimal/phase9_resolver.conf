# Phase 9 validates deterministic PJSIP IPv4 A/SRV resolution and resolved
# UDP transport routing against an application-local DNS responder.
CONFIG_PJLIB_UTIL=y
CONFIG_PJLIB_UTIL_DNS_RESOLVER=y
CONFIG_PJSIP=y
CONFIG_PJSIP_UDP_TRANSPORT=y
CONFIG_PJSIP_TCP_TRANSPORT=n
CONFIG_PJSIP_PHASE9_TEST=y

CONFIG_NET_IPV6=n
CONFIG_NET_LOOPBACK=y

# Two PJSIP UDP transports, one PJLIB-UTIL resolver socket, and one local DNS
# responder remain comfortably below these explicit descriptor limits.
CONFIG_NET_MAX_CONTEXTS=40
CONFIG_NET_MAX_CONN=40
CONFIG_ZVFS_OPEN_MAX=48
CONFIG_ZVFS_POLL_MAX=40
CONFIG_NET_BUF_DATA_SIZE=256
CONFIG_NET_BUF_RX_COUNT=32
CONFIG_NET_BUF_TX_COUNT=32
CONFIG_NET_PKT_RX_COUNT=16
CONFIG_NET_PKT_TX_COUNT=16

CONFIG_MAIN_STACK_SIZE=24576
CONFIG_HEAP_MEM_POOL_SIZE=393216
CONFIG_DYNAMIC_THREAD_POOL_SIZE=10
CONFIG_DYNAMIC_THREAD_STACK_SIZE=8192
CONFIG_MAX_PTHREAD_MUTEX_COUNT=64
