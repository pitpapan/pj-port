# Phase 4 compiles and links the audited 21-source PJSIP core. Socket-backed
# transports and endpoint runtime validation remain disabled here.
CONFIG_PJLIB_UTIL=y
CONFIG_PJSIP=y
CONFIG_PJSIP_UDP_TRANSPORT=n
CONFIG_PJSIP_TCP_TRANSPORT=n
CONFIG_PJSIP_PHASE4_TEST=y

CONFIG_NET_IPV6=n
