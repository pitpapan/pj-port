# Compile-only source-boundary check for the independently gated IPv4
# transports. No UDP or TCP runtime API is invoked in Phase 4.
CONFIG_PJLIB_UTIL=y
CONFIG_PJSIP=y
CONFIG_PJSIP_UDP_TRANSPORT=y
CONFIG_PJSIP_TCP_TRANSPORT=y
CONFIG_PJSIP_PHASE4_TEST=y

CONFIG_NET_IPV6=n
